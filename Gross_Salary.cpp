/**************************************************************************************************
************************************SOLUTION-BY-MOHIT**********************************************
***************************************************************************************************/
#include <bits/stdc++.h>
int main()
{
    int t; scanf("%d",&t);
    while(t--)
    {
        float salary; scanf("%f",&salary);
        if(salary < 1500)
        {
            salary = (salary - 0.1) + (salary - 0.9);
            printf("%f\n",salary); 
        }else if(salary >= 1500)
        {
            salary = (salary - 0.98) + 500;
            printf("%f\n",salary);
        }

    } 
    return 0;
}